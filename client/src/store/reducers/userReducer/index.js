import * as TYPE from "store/types";

const initialState = {
  credentials: null,
};

export const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
